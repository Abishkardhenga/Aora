import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const VideoCard = ({ video: { title, thumnail, prompt, video, users: { email, username } } }) => {
    return (
        <View className="flex-col items-center ">
            <Text></Text>
        </View>
    )
}

export default VideoCard

